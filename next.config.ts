import type { NextConfig } from "next";

/**
 * Next.js configuration file.
 *
 * `devIndicators: false` — hides the small "▲" build indicator overlay
 * that Next.js normally shows in the bottom-right corner during development.
 */
const nextConfig: NextConfig = {
  devIndicators: false,
};

export default nextConfig;
